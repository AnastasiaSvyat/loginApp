import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'login-app',
  webDir: 'dist/login-app',
  bundledWebRuntime: false
};

export default config;
